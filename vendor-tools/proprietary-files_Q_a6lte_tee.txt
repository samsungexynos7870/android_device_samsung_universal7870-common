###FILE###:proprietary-files_Q_a6lte_tee.txt
# TEE (MobiCore)
#
# libMcClient/libMcRegistry/mcDriverDaemon are taken from a6lte on purpose:
# they are the MobiCore client counterpart of the TbaseOS build shipped with
# the universal7870 BSP, and a6lte has no 64 bit variant (vendor is 32 bit
# only there). The widevine OEMCrypto blob from
# proprietary-files_Q_j7duolte_drm.txt links against this libMcClient.so, so
# both lists have to stay on the a6lte / j7duolte (Android 10) vintage.
vendor/bin/mcDriverDaemon
vendor/lib/libMcClient.so
vendor/lib/libMcRegistry.so

