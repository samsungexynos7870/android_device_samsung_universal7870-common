# CP-Boot Daemon
vendor_internal_prop(cpboot-daemon_prop)

# modemloader
vendor_internal_prop(modemloader_prop)

# sswap
vendor_internal_prop(sswap_prop)

# Fastcharge HAL
vendor_internal_prop(sec_fastcharge_prop)

# wifi
vendor_internal_prop(vendor_wlan_prop)

# bluetooth
vendor_internal_prop(vendor_bluetooth_prop)

# NFC
vendor_internal_prop(vendor_nfc_prop)

# OMX
vendor_internal_prop(vendor_omx_prop);

# radio
vendor_internal_prop(clatd_radio_prop)

# audio
vendor_internal_prop(vendor_audio_prop);
