# /system/lib/omx/
allow mediacodec system_file:dir r_file_perms;

get_prop(mediacodec vendor_omx_prop)

# /sys/class/video4linux/*
r_dir_file(mediacodec, sysfs_v4l);
r_dir_file(mediacodec, sysfs_camera)

allow mediacodec sysfs_v4l:dir search;
allow mediacodec m2m1shot_device:chr_file rw_file_perms;

hal_client_domain(mediacodec, hal_power)

# sysfs
allow mediacodec sysfs:file r_file_perms;
