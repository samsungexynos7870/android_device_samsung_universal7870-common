allow domain {
    debugfs_ion
    debugfs_mali
    debugfs_mali_mem
}:dir r_dir_perms;

dontaudit domain efs_file:lnk_file r_file_perms;

allow { domain -appdomain } system_data_root_file:dir read;
allow domain { debugfs_ion debugfs_mali debugfs_mali_mem }:dir search;
dontaudit domain postinstall_mnt_dir:dir getattr;
